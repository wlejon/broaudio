/* stb_vorbis implementation translation unit.
 *
 * Compiled as C: stb_vorbis.c relies on implicit void* conversions and does
 * not compile as C++. Consumers get the declarations by including
 * stb_vorbis.c with STB_VORBIS_HEADER_ONLY defined (see src/io/audio_file.cpp).
 */
#include "stb_vorbis/stb_vorbis.c"
